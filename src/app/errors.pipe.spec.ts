/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { ErrorsPipe } from './errors.pipe';

describe('Pipe: Errors', () => {
  it('create an instance', () => {
    let pipe = new ErrorsPipe();
    expect(pipe).toBeTruthy();
  });
});
